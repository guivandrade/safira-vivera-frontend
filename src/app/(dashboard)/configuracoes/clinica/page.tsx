import { ClinicSettingsPage } from '@/components/features/settings/ClinicSettingsPage';

export const metadata = {
  title: 'Clínica | Configurações',
};

export default function Page() {
  return <ClinicSettingsPage />;
}
