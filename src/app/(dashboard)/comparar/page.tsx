import { ComparePage } from '@/components/features/compare/ComparePage';

export const metadata = {
  title: 'Comparar períodos | Vivera',
};

export default function Page() {
  return <ComparePage />;
}
